# .github/workflows/build-mod-wiki.yml
name: 📚 Build Mod Wiki

on:
  push:
    branches: [main]
    paths:
      - 'jsons/**'
      - 'Images/**'
      - 'build_wiki.py'
      - '.github/workflows/build-mod-wiki.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run wiki builder
        run: python build_wiki.py

      # ⚠️ 不再部署！GitHub Pages 直接读取 main/docs